version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: air-quality-api
    ports:
      - "8000:8000"
    environment:
      - AQICN_API_TOKEN=${AQICN_API_TOKEN:-70fa7c8239bbf05b4090b6992f04d1047fc96267}
      - CACHE_TTL=${CACHE_TTL:-300}
      - RATE_LIMIT_PER_MINUTE=${RATE_LIMIT_PER_MINUTE:-60}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - ML_MODEL_TYPE=${ML_MODEL_TYPE:-linear}
    volumes:
      - ./backend:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Optional: Add nginx for serving frontend
  # frontend:
  #   image: nginx:alpine
  #   container_name: air-quality-frontend
  #   ports:
  #     - "8080:80"
  #   volumes:
  #     - ./frontend:/usr/share/nginx/html:ro
  #   depends_on:
  #     - backend



